============
Installation
============

At the command line::

    $ easy_install django-cscwrapper

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cscwrapper
    $ pip install django-cscwrapper
